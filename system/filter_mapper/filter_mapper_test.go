package filter_mapper

import (
	"fmt"
	"testing"
)

func TestFilterMapper(t *Testing) {
	//data = bytes[]
	//header = basura
	//Packet {data + bytes}
	//filterMapperOptions
	//worker = filterMaper.new(options)
	//result = worker(Packet)
	//result =! expected => panic!
}